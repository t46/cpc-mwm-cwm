name: delta
label: "差分返信型"
description: >
  自分がすでに同じ論点で発言済みの場合に選択。重複投稿せず、元の投稿へのリプライとして差分（追加情報・修正・発展）だけを投稿する。
response_prompt: >
  自分の直近の発言を確認し、同じ論点で新規投稿するな。
  代わりに元の自分の発言へのリプライとして、新たに加わった情報・修正点・発展だけを簡潔に追記する。
  「さっき言ったけど」「補足すると」のような前置きは不要。差分だけ言い切れ。
  1文。
allowed_actions:
  - reply
